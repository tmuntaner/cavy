%h1 Website Pages

%table.table.table-striped
  %thead
    %tr
      %th Page
      %th Title
      %th Render
      %th 
      %th 
  %tbody
    - Cavy::Page.all.each do |page|
      %tr
        %th= page.id
        %th= link_to page.title, admin_page_path(page.id), id: "page-#{page.title}"
        %th= page.render
        %th= link_to 'edit', admin_edit_page_path(page.id), id: "edit-page-#{page.id}"
        %th= link_to 'destroy', admin_delete_page_path(page.id), id: "delete-page-#{page.id}", method: :delete, confirm: "Are you sure you want to delete the page?"